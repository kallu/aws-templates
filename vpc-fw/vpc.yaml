




---
AWSTemplateFormatVersion: '2010-09-09'
Description: Inspection + Egress VPCs v1.0.0

Metadata:
  AWS::CloudFormation::Interface:
    ParameterGroups:
      -
        Label:
          default: Internal Inspection VPC
        Parameters:
          - InternalInspectionVpcCidr
      -
        Label:
          default: External Inspection VPC
        Parameters:
          - ExternalInspectionVpcCidr
      -
        Label:
          default: Egress VPC
        Parameters:
          - EgressVpcCidr
          - EipA
          - EipB
          - EipC
      -
        Label:
          default: TGW Configuration
        Parameters:
          - TgwAttachment
          - TgwRouteCidr1
          - TgwRouteCidr2
          - TgwRouteCidr3

Parameters:

  EgressVpcCidr:
    Type: String
    Description: Egress VPC CIDR
    Default: '10.0.0.0/25'
    AllowedPattern: "^(([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])(\\/(1[6-9]|2[0-8]))$"
  EipA:
    Type: String
    Description: Pre-allocated EIP ID for AZ-A (optional)
    Default: ''

  EipB:
    Type: String
    Description: Pre-allocated EIP ID for AZ-B (optional)
    Default: ''

  EipC:
    Type: String
    Description: Pre-allocated EIP ID for AZ-C (optional)
    Default: ''


  InternalInspectionVpcCidr:
    Type: String
    Description: Internal Inspection VPC CIDR
    Default: '100.64.0.0/25'
    AllowedPattern: "^(([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])(\\/(1[6-9]|2[0-8]))$"

  ExternalInspectionVpcCidr:
    Type: String
    Description: External Inspection VPC CIDR
    Default: '100.64.128.0/25'
    AllowedPattern: "^(([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])(\\/(1[6-9]|2[0-8]))$"

  TgwAttachment:
    Type: String
    Description: TGW ID attached to VPCs (optional)
    Default: ''
  TgwRouteCidr1:
    Type: String
    Description: CIDR to be routed from Egress to TGW (optional)
    Default: ''
    AllowedPattern: "^((([01]?[0-9]?[0-9]|2([0-4][0-9]|5[0-5]))\\.){3}([01]?[0-9]?[0-9]|2([0-4][0-9]|5[0-5]))/([0-2]?[0-9]|3[0-2])){0,1}$"
  TgwRouteCidr2:
    Type: String
    Description: CIDR to be routed from Egress to TGW (optional)
    Default: ''
    AllowedPattern: "^((([01]?[0-9]?[0-9]|2([0-4][0-9]|5[0-5]))\\.){3}([01]?[0-9]?[0-9]|2([0-4][0-9]|5[0-5]))/([0-2]?[0-9]|3[0-2])){0,1}$"
  TgwRouteCidr3:
    Type: String
    Description: CIDR to be routed from Egress to TGW (optional)
    Default: ''
    AllowedPattern: "^((([01]?[0-9]?[0-9]|2([0-4][0-9]|5[0-5]))\\.){3}([01]?[0-9]?[0-9]|2([0-4][0-9]|5[0-5]))/([0-2]?[0-9]|3[0-2])){0,1}$"

Conditions:
  CreateEipA: !Equals [!Ref EipA, "" ]
  CreateEipB: !Equals [!Ref EipB, "" ]
  CreateEipC: !Equals [!Ref EipC, "" ]
  HasTgwAttachment: !Not [ !Equals [!Ref TgwAttachment, "" ] ]
  HasTgwRouteCidr1: !Not [ !Equals [!Ref TgwRouteCidr1, "" ] ]
  AddTgwRoute1: !And [ Condition: HasTgwAttachment, Condition: HasTgwRouteCidr1 ]
  HasTgwRouteCidr2: !Not [ !Equals [!Ref TgwRouteCidr2, "" ] ]
  AddTgwRoute2: !And [ Condition: HasTgwAttachment, Condition: HasTgwRouteCidr2 ]
  HasTgwRouteCidr3: !Not [ !Equals [!Ref TgwRouteCidr3, "" ] ]
  AddTgwRoute3: !And [ Condition: HasTgwAttachment, Condition: HasTgwRouteCidr3 ]
  
Resources:

  InternalInspectionVPC:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: !Ref InternalInspectionVpcCidr
      EnableDnsSupport: true
      EnableDnsHostnames: true
      InstanceTenancy: default
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Internal Inspection VPC'

  ExternalInspectionVPC:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: !Ref ExternalInspectionVpcCidr
      EnableDnsSupport: true
      EnableDnsHostnames: true
      InstanceTenancy: default
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} On-prem Inspection VPC'

  EgressVPC:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: !Ref EgressVpcCidr
      EnableDnsSupport: true
      EnableDnsHostnames: true
      InstanceTenancy: default
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC'

  InetGW:
    Type: AWS::EC2::InternetGateway
    Properties:
      Tags:
      - Key: Name
        Value: !Sub '${AWS::StackName} Egress VPC Internet gateway'

  InetGWattachment:
    Type: AWS::EC2::VPCGatewayAttachment
    Properties:
      VpcId: !Ref EgressVPC
      InternetGatewayId: !Ref InetGW

  PubRouteTable:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref EgressVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC Public route-table'

  PubDefaultRoute:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref PubRouteTable
      DestinationCidrBlock: '0.0.0.0/0'
      GatewayId: !Ref InetGW

  InternalInspectionTgwAttachment:
    Type: AWS::EC2::TransitGatewayVpcAttachment
    Condition: HasTgwAttachment
    Properties:
      VpcId: !Ref InternalInspectionVPC
      TransitGatewayId: !Ref TgwAttachment
      Tags:
        - Key: "Name"
          Value: !Sub '${AWS::StackName} Internal Inspection VPC'
      SubnetIds:
        - !Ref InternalInspectionTGWSubnetA
        - !Ref InternalInspectionTGWSubnetB
        - !Ref InternalInspectionTGWSubnetC

  ExternalInspectionTgwAttachment:
    Type: AWS::EC2::TransitGatewayVpcAttachment
    Condition: HasTgwAttachment
    Properties:
      VpcId: !Ref ExternalInspectionVPC
      TransitGatewayId: !Ref TgwAttachment
      Tags:
        - Key: "Name"
          Value: !Sub '${AWS::StackName} On-Prem Inspection VPC'
      SubnetIds:
        - !Ref ExternalInspectionTGWSubnetA
        - !Ref ExternalInspectionTGWSubnetB
        - !Ref ExternalInspectionTGWSubnetC

  EgressTgwAttachment:
    Type: AWS::EC2::TransitGatewayVpcAttachment
    Condition: HasTgwAttachment
    Properties:
      VpcId: !Ref EgressVPC
      TransitGatewayId: !Ref TgwAttachment
      Tags:
        - Key: "Name"
          Value: !Sub '${AWS::StackName} Egress VPC'
      SubnetIds:
        - !Ref EgressTGWSubnetA
        - !Ref EgressTGWSubnetB
        - !Ref EgressTGWSubnetC

  PubTGWRoute1:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute1
    DependsOn: InternalInspectionTgwAttachment
    Properties:
      RouteTableId: !Ref PubRouteTable
      DestinationCidrBlock: !Ref TgwRouteCidr1
      TransitGatewayId: !Ref TgwAttachment
  PubTGWRoute2:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute2
    DependsOn: InternalInspectionTgwAttachment
    Properties:
      RouteTableId: !Ref PubRouteTable
      DestinationCidrBlock: !Ref TgwRouteCidr2
      TransitGatewayId: !Ref TgwAttachment
  PubTGWRoute3:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute3
    DependsOn: InternalInspectionTgwAttachment
    Properties:
      RouteTableId: !Ref PubRouteTable
      DestinationCidrBlock: !Ref TgwRouteCidr3
      TransitGatewayId: !Ref TgwAttachment
  InternalFwRouteTable:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref InternalInspectionVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Internal Inspection VPC FW route-table'

  InternalFwDefaultRoute:
    Type: AWS::EC2::Route
    Condition: HasTgwAttachment
    DependsOn: InternalInspectionTgwAttachment
    Properties:
      RouteTableId: !Ref InternalFwRouteTable
      DestinationCidrBlock: '0.0.0.0/0'
      TransitGatewayId: !Ref TgwAttachment

  InternalNetworkFW:
    Type: AWS::NetworkFirewall::Firewall
    Properties:
      FirewallName: !Sub 'Internal ${AWS::StackName}'
      FirewallPolicyArn: !Ref InternalDefaultFirewallPolicy
      VpcId: !Ref InternalInspectionVPC
      SubnetMappings:
        - SubnetId: !Ref InternalInspectionFWSubnetA
        - SubnetId: !Ref InternalInspectionFWSubnetB
        - SubnetId: !Ref InternalInspectionFWSubnetC
      Description: Network firewall to inspect traffic to on-prem networks
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} on-prem network firewall'
  # See https://github.com/aws-cloudformation/aws-cloudformation-resource-providers-networkfirewall/issues/15
  InternalInspectionTGWRouteA:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref InternalInspectionTGWRouteTableA
      DestinationCidrBlock: '0.0.0.0/0'
      VpcEndpointId: !Select [ 0, !Split [ ',', !Select [ 1 , !Split [ 'a:', !Join [ ',', !GetAtt InternalNetworkFW.EndpointIds ] ] ] ] ]
  # See https://github.com/aws-cloudformation/aws-cloudformation-resource-providers-networkfirewall/issues/15
  InternalInspectionTGWRouteB:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref InternalInspectionTGWRouteTableB
      DestinationCidrBlock: '0.0.0.0/0'
      VpcEndpointId: !Select [ 0, !Split [ ',', !Select [ 1 , !Split [ 'b:', !Join [ ',', !GetAtt InternalNetworkFW.EndpointIds ] ] ] ] ]
  # See https://github.com/aws-cloudformation/aws-cloudformation-resource-providers-networkfirewall/issues/15
  InternalInspectionTGWRouteC:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref InternalInspectionTGWRouteTableC
      DestinationCidrBlock: '0.0.0.0/0'
      VpcEndpointId: !Select [ 0, !Split [ ',', !Select [ 1 , !Split [ 'c:', !Join [ ',', !GetAtt InternalNetworkFW.EndpointIds ] ] ] ] ]

  InternalDefaultFirewallPolicy:
    Type: 'AWS::NetworkFirewall::FirewallPolicy'
    Properties:
      FirewallPolicyName: !Sub "${AWS::StackName}-default-Internal-policy"
      FirewallPolicy:
        StatelessDefaultActions:
          - 'aws:pass'
        StatelessFragmentDefaultActions:
          - 'aws:pass'
      Description: Default firewall policy passing everything
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} default on-prem policy'

# ADD DEFAULT POLICY AND RuleGroup
# Should there be 1 NFW and then fix the cross AZ routing manually?

#  SampleStatefulRulegroup:
#    Type: 'AWS::NetworkFirewall::RuleGroup'
#    Properties:
#      RuleGroupName: SampleStatefulRulegroupName
#      Type: STATEFUL
#      RuleGroup:
#        RulesSource:
#          RulesString: >-
#            pass tcp 10.20.20.0/24 45400:45500 <> 10.10.10.0/24 5203
#            (msg:"test";sid:1;rev:1;)
#      Capacity: 100
#      Description: Rulegroup description goes here
#      Tags:
#        - Key: Foo
#          Value: Bar

  ExternalFwRouteTable:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref ExternalInspectionVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} External Inspection VPC FW route-table'

  ExternalFwDefaultRoute:
    Type: AWS::EC2::Route
    Condition: HasTgwAttachment
    DependsOn: ExternalInspectionTgwAttachment
    Properties:
      RouteTableId: !Ref ExternalFwRouteTable
      DestinationCidrBlock: '0.0.0.0/0'
      TransitGatewayId: !Ref TgwAttachment

  ExternalNetworkFW:
    Type: AWS::NetworkFirewall::Firewall
    Properties:
      FirewallName: !Sub 'External ${AWS::StackName}'
      FirewallPolicyArn: !Ref ExternalDefaultFirewallPolicy
      VpcId: !Ref ExternalInspectionVPC
      SubnetMappings:
        - SubnetId: !Ref ExternalInspectionFWSubnetA
        - SubnetId: !Ref ExternalInspectionFWSubnetB
        - SubnetId: !Ref ExternalInspectionFWSubnetC
      Description: Network firewall to inspect traffic to on-prem networks
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} on-prem network firewall'
  # See https://github.com/aws-cloudformation/aws-cloudformation-resource-providers-networkfirewall/issues/15
  ExternalInspectionTGWRouteA:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref ExternalInspectionTGWRouteTableA
      DestinationCidrBlock: '0.0.0.0/0'
      VpcEndpointId: !Select [ 0, !Split [ ',', !Select [ 1 , !Split [ 'a:', !Join [ ',', !GetAtt ExternalNetworkFW.EndpointIds ] ] ] ] ]
  # See https://github.com/aws-cloudformation/aws-cloudformation-resource-providers-networkfirewall/issues/15
  ExternalInspectionTGWRouteB:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref ExternalInspectionTGWRouteTableB
      DestinationCidrBlock: '0.0.0.0/0'
      VpcEndpointId: !Select [ 0, !Split [ ',', !Select [ 1 , !Split [ 'b:', !Join [ ',', !GetAtt ExternalNetworkFW.EndpointIds ] ] ] ] ]
  # See https://github.com/aws-cloudformation/aws-cloudformation-resource-providers-networkfirewall/issues/15
  ExternalInspectionTGWRouteC:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref ExternalInspectionTGWRouteTableC
      DestinationCidrBlock: '0.0.0.0/0'
      VpcEndpointId: !Select [ 0, !Split [ ',', !Select [ 1 , !Split [ 'c:', !Join [ ',', !GetAtt ExternalNetworkFW.EndpointIds ] ] ] ] ]

  ExternalDefaultFirewallPolicy:
    Type: 'AWS::NetworkFirewall::FirewallPolicy'
    Properties:
      FirewallPolicyName: !Sub "${AWS::StackName}-default-External-policy"
      FirewallPolicy:
        StatelessDefaultActions:
          - 'aws:pass'
        StatelessFragmentDefaultActions:
          - 'aws:pass'
      Description: Default firewall policy passing everything
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} default on-prem policy'

# ADD DEFAULT POLICY AND RuleGroup
# Should there be 1 NFW and then fix the cross AZ routing manually?

#  SampleStatefulRulegroup:
#    Type: 'AWS::NetworkFirewall::RuleGroup'
#    Properties:
#      RuleGroupName: SampleStatefulRulegroupName
#      Type: STATEFUL
#      RuleGroup:
#        RulesSource:
#          RulesString: >-
#            pass tcp 10.20.20.0/24 45400:45500 <> 10.10.10.0/24 5203
#            (msg:"test";sid:1;rev:1;)
#      Capacity: 100
#      Description: Rulegroup description goes here
#      Tags:
#        - Key: Foo
#          Value: Bar

  EgressTGWSubnetA:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref EgressVPC
      AvailabilityZone: !Sub ${AWS::Region}a
      CidrBlock: !Select [ 0 , !Cidr [ !Ref EgressVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC TGW subnet-A'

  EgressPubSubnetA:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref EgressVPC
      AvailabilityZone: !Sub ${AWS::Region}a
      CidrBlock: !Select [ 4 , !Cidr [ !Ref EgressVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC Public subnet-A'

  InternalInspectionTGWSubnetA:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref InternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}a
      CidrBlock: !Select [ 0 , !Cidr [ !Ref InternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Internal Inspection VPC TGW subnet-A'

  InternalInspectionFWSubnetA:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref InternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}a
      CidrBlock: !Select [ 4 , !Cidr [ !Ref InternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Internal Inspection VPC FW subnet-A'

  ExternalInspectionTGWSubnetA:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref ExternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}a
      CidrBlock: !Select [ 0 , !Cidr [ !Ref ExternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} On-Prem Inspection VPC TGW subnet-A'

  ExternalInspectionFWSubnetA:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref ExternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}a
      CidrBlock: !Select [ 4 , !Cidr [ !Ref ExternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} On-Prem Inspection VPC FW subnet-A'
  NatGwA:
    Type: AWS::EC2::NatGateway
    Properties:
      ConnectivityType: public
      SubnetId: !Ref EgressPubSubnetA
      AllocationId: !If [ CreateEipA, !GetAtt NatEipA.AllocationId, !Ref EipA ]
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC NAT-A'

  NatEipA:
    Type: AWS::EC2::EIP
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Condition: CreateEipA
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC NAT-A'

  EgressTGWRouteTableA:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref EgressVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC TGW route-table-A'

  EgressTGWSubnetRoutingA:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableA
      SubnetId: !Ref EgressTGWSubnetA

  EgressTGWDefaultRouteA:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableA
      DestinationCidrBlock: '0.0.0.0/0'
      NatGatewayId: !Ref NatGwA

  EgressPubSubnetRoutingA:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref PubRouteTable
      SubnetId: !Ref EgressPubSubnetA

  InternalInspectionTGWRouteTableA:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref InternalInspectionVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Internal Inspection VPC TGW route-table-A'

  InspectionTGWSubnetRoutingA:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref InternalInspectionTGWRouteTableA
      SubnetId: !Ref InternalInspectionTGWSubnetA

  InspectionTGWSFWRoutingA:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref InternalFwRouteTable
      SubnetId: !Ref InternalInspectionFWSubnetA

  ExternalInspectionTGWRouteTableA:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref ExternalInspectionVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} On-Prem Inspection VPC TGW route-table-A'

  ExternalInspectionTGWSubnetRoutingA:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref ExternalInspectionTGWRouteTableA
      SubnetId: !Ref ExternalInspectionTGWSubnetA

  ExternalInspectionTGWSFWRoutingA:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref ExternalFwRouteTable
      SubnetId: !Ref ExternalInspectionFWSubnetA

  EgressTGWRouteA1:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute1
    DependsOn: EgressTgwAttachment
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableA
      DestinationCidrBlock: !Ref TgwRouteCidr1
      TransitGatewayId: !Ref TgwAttachment
  EgressTGWRouteA2:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute2
    DependsOn: EgressTgwAttachment
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableA
      DestinationCidrBlock: !Ref TgwRouteCidr2
      TransitGatewayId: !Ref TgwAttachment
  EgressTGWRouteA3:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute3
    DependsOn: EgressTgwAttachment
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableA
      DestinationCidrBlock: !Ref TgwRouteCidr3
      TransitGatewayId: !Ref TgwAttachment
  EgressTGWSubnetB:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref EgressVPC
      AvailabilityZone: !Sub ${AWS::Region}b
      CidrBlock: !Select [ 1 , !Cidr [ !Ref EgressVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC TGW subnet-B'

  EgressPubSubnetB:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref EgressVPC
      AvailabilityZone: !Sub ${AWS::Region}b
      CidrBlock: !Select [ 5 , !Cidr [ !Ref EgressVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC Public subnet-B'

  InternalInspectionTGWSubnetB:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref InternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}b
      CidrBlock: !Select [ 1 , !Cidr [ !Ref InternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Internal Inspection VPC TGW subnet-B'

  InternalInspectionFWSubnetB:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref InternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}b
      CidrBlock: !Select [ 5 , !Cidr [ !Ref InternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Internal Inspection VPC FW subnet-B'

  ExternalInspectionTGWSubnetB:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref ExternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}b
      CidrBlock: !Select [ 1 , !Cidr [ !Ref ExternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} On-Prem Inspection VPC TGW subnet-B'

  ExternalInspectionFWSubnetB:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref ExternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}b
      CidrBlock: !Select [ 5 , !Cidr [ !Ref ExternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} On-Prem Inspection VPC FW subnet-B'
  NatGwB:
    Type: AWS::EC2::NatGateway
    Properties:
      ConnectivityType: public
      SubnetId: !Ref EgressPubSubnetB
      AllocationId: !If [ CreateEipB, !GetAtt NatEipB.AllocationId, !Ref EipB ]
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC NAT-B'

  NatEipB:
    Type: AWS::EC2::EIP
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Condition: CreateEipB
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC NAT-B'

  EgressTGWRouteTableB:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref EgressVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC TGW route-table-B'

  EgressTGWSubnetRoutingB:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableB
      SubnetId: !Ref EgressTGWSubnetB

  EgressTGWDefaultRouteB:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableB
      DestinationCidrBlock: '0.0.0.0/0'
      NatGatewayId: !Ref NatGwB

  EgressPubSubnetRoutingB:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref PubRouteTable
      SubnetId: !Ref EgressPubSubnetB

  InternalInspectionTGWRouteTableB:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref InternalInspectionVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Internal Inspection VPC TGW route-table-B'

  InspectionTGWSubnetRoutingB:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref InternalInspectionTGWRouteTableB
      SubnetId: !Ref InternalInspectionTGWSubnetB

  InspectionTGWSFWRoutingB:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref InternalFwRouteTable
      SubnetId: !Ref InternalInspectionFWSubnetB

  ExternalInspectionTGWRouteTableB:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref ExternalInspectionVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} On-Prem Inspection VPC TGW route-table-B'

  ExternalInspectionTGWSubnetRoutingB:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref ExternalInspectionTGWRouteTableB
      SubnetId: !Ref ExternalInspectionTGWSubnetB

  ExternalInspectionTGWSFWRoutingB:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref ExternalFwRouteTable
      SubnetId: !Ref ExternalInspectionFWSubnetB

  EgressTGWRouteB1:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute1
    DependsOn: EgressTgwAttachment
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableB
      DestinationCidrBlock: !Ref TgwRouteCidr1
      TransitGatewayId: !Ref TgwAttachment
  EgressTGWRouteB2:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute2
    DependsOn: EgressTgwAttachment
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableB
      DestinationCidrBlock: !Ref TgwRouteCidr2
      TransitGatewayId: !Ref TgwAttachment
  EgressTGWRouteB3:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute3
    DependsOn: EgressTgwAttachment
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableB
      DestinationCidrBlock: !Ref TgwRouteCidr3
      TransitGatewayId: !Ref TgwAttachment
  EgressTGWSubnetC:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref EgressVPC
      AvailabilityZone: !Sub ${AWS::Region}c
      CidrBlock: !Select [ 2 , !Cidr [ !Ref EgressVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC TGW subnet-C'

  EgressPubSubnetC:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref EgressVPC
      AvailabilityZone: !Sub ${AWS::Region}c
      CidrBlock: !Select [ 6 , !Cidr [ !Ref EgressVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC Public subnet-C'

  InternalInspectionTGWSubnetC:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref InternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}c
      CidrBlock: !Select [ 2 , !Cidr [ !Ref InternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Internal Inspection VPC TGW subnet-C'

  InternalInspectionFWSubnetC:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref InternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}c
      CidrBlock: !Select [ 6 , !Cidr [ !Ref InternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Internal Inspection VPC FW subnet-C'

  ExternalInspectionTGWSubnetC:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref ExternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}c
      CidrBlock: !Select [ 2 , !Cidr [ !Ref ExternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} On-Prem Inspection VPC TGW subnet-C'

  ExternalInspectionFWSubnetC:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref ExternalInspectionVPC
      AvailabilityZone: !Sub ${AWS::Region}c
      CidrBlock: !Select [ 6 , !Cidr [ !Ref ExternalInspectionVpcCidr, 8, 4 ] ]
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} On-Prem Inspection VPC FW subnet-C'
  NatGwC:
    Type: AWS::EC2::NatGateway
    Properties:
      ConnectivityType: public
      SubnetId: !Ref EgressPubSubnetC
      AllocationId: !If [ CreateEipC, !GetAtt NatEipC.AllocationId, !Ref EipC ]
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC NAT-C'

  NatEipC:
    Type: AWS::EC2::EIP
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Condition: CreateEipC
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC NAT-C'

  EgressTGWRouteTableC:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref EgressVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Egress VPC TGW route-table-C'

  EgressTGWSubnetRoutingC:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableC
      SubnetId: !Ref EgressTGWSubnetC

  EgressTGWDefaultRouteC:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableC
      DestinationCidrBlock: '0.0.0.0/0'
      NatGatewayId: !Ref NatGwC

  EgressPubSubnetRoutingC:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref PubRouteTable
      SubnetId: !Ref EgressPubSubnetC

  InternalInspectionTGWRouteTableC:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref InternalInspectionVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} Internal Inspection VPC TGW route-table-C'

  InspectionTGWSubnetRoutingC:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref InternalInspectionTGWRouteTableC
      SubnetId: !Ref InternalInspectionTGWSubnetC

  InspectionTGWSFWRoutingC:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref InternalFwRouteTable
      SubnetId: !Ref InternalInspectionFWSubnetC

  ExternalInspectionTGWRouteTableC:
    Type: AWS::EC2::RouteTable
    Properties: 
      VpcId: !Ref ExternalInspectionVPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName} On-Prem Inspection VPC TGW route-table-C'

  ExternalInspectionTGWSubnetRoutingC:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref ExternalInspectionTGWRouteTableC
      SubnetId: !Ref ExternalInspectionTGWSubnetC

  ExternalInspectionTGWSFWRoutingC:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      RouteTableId: !Ref ExternalFwRouteTable
      SubnetId: !Ref ExternalInspectionFWSubnetC

  EgressTGWRouteC1:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute1
    DependsOn: EgressTgwAttachment
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableC
      DestinationCidrBlock: !Ref TgwRouteCidr1
      TransitGatewayId: !Ref TgwAttachment
  EgressTGWRouteC2:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute2
    DependsOn: EgressTgwAttachment
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableC
      DestinationCidrBlock: !Ref TgwRouteCidr2
      TransitGatewayId: !Ref TgwAttachment
  EgressTGWRouteC3:
    Type: AWS::EC2::Route
    Condition: AddTgwRoute3
    DependsOn: EgressTgwAttachment
    Properties:
      RouteTableId: !Ref EgressTGWRouteTableC
      DestinationCidrBlock: !Ref TgwRouteCidr3
      TransitGatewayId: !Ref TgwAttachment

Outputs:

  EgressVpcId:
    Description: VPC ID
    Value: !Ref EgressVPC
    Export:
      Name: !Sub '${AWS::StackName}-EgressVpcId'

  InternalInspectionVpcId:
    Description: VPC ID
    Value: !Ref InternalInspectionVPC
    Export:
      Name: !Sub '${AWS::StackName}-InternalInspectionVpcId'

  ExternalInspectionVpcId:
    Description: VPC ID
    Value: !Ref ExternalInspectionVPC
    Export:
      Name: !Sub '${AWS::StackName}-ExternalInspectionVpcId'
